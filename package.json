{
  "name": "url-shortener-microservice",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "NODE_ENV=test nodemon src/index.js",
    "test": "NODE_ENV=test nodemon --exec 'mocha --recursive -R min src/test'",
    "test-travis": "NODE_ENV=test mocha --recursive src/test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.1",
    "mongoose": "^4.12.0"
  },
  "devDependencies": {
    "mocha": "^4.0.0",
    "nodemon": "^1.12.1",
    "supertest": "^3.0.0"
  }
}
